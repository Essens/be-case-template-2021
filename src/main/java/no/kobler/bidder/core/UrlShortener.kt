package no.kobler.bidder.core

import java.util.Optional

class BidderService {
    fun shorten(url: String?): String {
        return ""
    }

    fun findUrl(id: String?): Optional<String> {
        return Optional.empty()
    }
}
